<h1>Editing event_photo</h1>

<% form_for(@event_photo) do |f| %>
  <%= f.error_messages %>

  <p>
    <%= f.submit "Update" %>
  </p>
<% end %>

<%= link_to 'Show', @event_photo %> |
<%= link_to 'Back', event_photos_path %>
